'''

.. code-block:: python

    >>> from datafs.managers.manager_mongo import MongoDBManager
    >>> from fs.osfs import OSFS
    >>> import os
    >>> import tempfile
    >>> import shutil
    >>> import glob


'''